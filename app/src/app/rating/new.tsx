import { RatingComposer } from '@/features/rating/rating-composer';

/** `/rating/new` — presented as a modal from the raised button in the tab bar. */
export default function NewRatingScreen() {
  return <RatingComposer />;
}
